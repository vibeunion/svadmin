{
  "surface": "0.9.x",
  "minimumSupported": {
    "@svadmin/core": "0.53.0",
    "@svadmin/ui": "0.73.0",
    "svelte": "5.56.10"
  },
  "testedCombinations": [
    {
      "name": "minimum-supported",
      "core": "0.53.0",
      "ui": "0.73.0",
      "svelte": "5.56.10",
      "source": "Exact candidate tarballs when workspace versions equal these minima; otherwise install the exact registry minima. CI must pass; this is not a publication receipt."
    },
    {
      "name": "workspace-packed",
      "core": "workspace tarball",
      "ui": "workspace tarball",
      "svelte": "workspace override"
    }
  ],
  "minimumSupportedAppliesTo": [
    ".",
    "./svelte",
    "./interactive"
  ],
  "entrypointCompatibility": {
    "./interactive": {
      "status": "experimental",
      "requiredUiCapabilities": [
        "JsonSchemaForm.idPrefix",
        "proxy-safe nested field updates",
        "required boolean and decimal input support"
      ],
      "minimumPublishedUi": null,
      "verification": "scripts/check-package-packs.ts validates exact installed minima, strict peers and the interactive browser import; scripts/surface-workflows/packed-consumer.mjs checks the full same-checkout workflow. Read the final-head CI results.",
      "releaseGate": "Publish the dependency-closed release with @svadmin/core@0.53.0 and @svadmin/ui@0.73.0 before Surface. The package peer floors enforce this requirement. minimumPublishedUi remains null until registry publication is independently verified; candidate tarballs are not registry evidence.",
      "minimumRequiredUi": "0.73.0"
    },
    "./server/sqlite": {
      "runtime": "Node.js",
      "verification": "Current CI Node version with real node:sqlite; no browser or distributed-store support implied."
    }
  }
}
