<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLButtonAttributes } from 'svelte/elements'; import { Trash2 } from '@lucide/svelte'; import { cn } from '../../utils.js'; import { useTerminalContext } from './context.svelte.js'; interface Props extends Omit<HTMLButtonAttributes, 'children' | 'class'> { class?: string; children?: Snippet; } let { class: className = '', children, ...rest }: Props = $props(); const terminal = useTerminalContext('TerminalClearButton');</script>
{#if terminal.onClear}<button {...rest} type="button" class={cn('svadmin-ai-terminal-part__button', className)} aria-label="Clear terminal" title="Clear terminal" data-slot="terminal-clear-button" onclick={() => terminal.onClear?.()}>{#if children}{@render children()}{:else}<Trash2 size={14} aria-hidden="true" />{/if}</button>{/if}
<style>.svadmin-ai-terminal-part__button { display: inline-flex; width: 1.9rem; height: 1.9rem; align-items: center; justify-content: center; border: 0; border-radius: min(var(--radius, .5rem), .5rem); background: transparent; color: var(--muted-foreground, currentColor); cursor: pointer; }.svadmin-ai-terminal-part__button:hover { background: var(--muted, transparent); color: var(--foreground, currentColor); }.svadmin-ai-terminal-part__button:focus-visible { outline: 2px solid var(--ring, currentColor); outline-offset: 2px; }</style>
