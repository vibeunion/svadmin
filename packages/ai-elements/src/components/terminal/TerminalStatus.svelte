<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import { useTerminalContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLDivElement>, 'children' | 'class'> { class?: string; children?: Snippet; } let { class: className = '', children, ...rest }: Props = $props(); const terminal = useTerminalContext('TerminalStatus');</script>
{#if terminal.isStreaming}<div {...rest} class={cn('svadmin-ai-terminal-part__status', className)} data-slot="terminal-status" role="status" aria-live="polite">{#if children}{@render children()}{:else}<span class="svadmin-ai-terminal-part__pulse" aria-hidden="true"></span>Running{/if}</div>{/if}
<style>.svadmin-ai-terminal-part__status { display: inline-flex; align-items: center; gap: .35rem; color: var(--muted-foreground, currentColor); font-size: .7rem; }.svadmin-ai-terminal-part__pulse { width: .4rem; height: .4rem; border-radius: 50%; background: var(--primary, currentColor); animation: svadmin-ai-terminal-pulse 1s ease-in-out infinite; } @keyframes svadmin-ai-terminal-pulse { 50% { opacity: .35; } }</style>
