<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { Terminal as TerminalIcon } from '@lucide/svelte'; import { cn } from '../../utils.js'; interface Props extends Omit<HTMLAttributes<HTMLDivElement>, 'children' | 'class'> { class?: string; children?: Snippet; } let { class: className = '', children, ...rest }: Props = $props();</script>
<div {...rest} class={cn('svadmin-ai-terminal-part__title', className)} data-slot="terminal-title"><TerminalIcon size={15} aria-hidden="true" />{#if children}{@render children()}{:else}Terminal{/if}</div>
<style>.svadmin-ai-terminal-part__title { display: inline-flex; align-items: center; gap: .45rem; color: var(--muted-foreground, currentColor); font-size: .78rem; }</style>
