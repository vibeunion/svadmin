<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; interface Props extends Omit<HTMLAttributes<HTMLPreElement>, 'children' | 'class'> { class?: string; children?: Snippet; value?: unknown; } let { class: className = '', children, value, ...rest }: Props = $props(); const formatted = $derived(typeof value === 'string' ? value : value === undefined ? '' : JSON.stringify(value, null, 2));</script>
<pre {...rest} class={cn('svadmin-ai-schema-part__example', className)} data-slot="schema-display-example">{#if children}{@render children()}{:else}{formatted}{/if}</pre>
<style>.svadmin-ai-schema-part__example { margin: .75rem; overflow: auto; padding: .75rem; border-radius: min(var(--radius, .5rem), .4rem); background: var(--muted, transparent); font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .72rem; line-height: 1.5; }</style>
