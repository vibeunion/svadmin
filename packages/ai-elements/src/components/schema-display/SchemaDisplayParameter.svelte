<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import type { SchemaParameter } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLDivElement>, 'children' | 'class'>, SchemaParameter { class?: string; children?: Snippet<[SchemaParameter]>; } let { name, type, required = false, description, location, class: className = '', children, ...rest }: Props = $props(); const parameter = $derived({ name, type, required, description, location });</script>
<div {...rest} class={cn('svadmin-ai-schema-part__parameter', className)} data-slot="schema-display-parameter">{#if children}{@render children(parameter)}{:else}<div><code>{name}</code><span>{type}</span>{#if location}<span>{location}</span>{/if}{#if required}<strong>required</strong>{/if}</div>{#if description}<p>{description}</p>{/if}{/if}</div>
<style>.svadmin-ai-schema-part__parameter { display: grid; gap: .3rem; padding: .65rem .75rem .65rem 2.5rem; }.svadmin-ai-schema-part__parameter > div { display: flex; flex-wrap: wrap; align-items: center; gap: .4rem; }.svadmin-ai-schema-part__parameter code { font-size: .75rem; }.svadmin-ai-schema-part__parameter span, .svadmin-ai-schema-part__parameter strong { padding: .08rem .35rem; border: 1px solid var(--border, currentColor); border-radius: .25rem; color: var(--muted-foreground, currentColor); font-size: .65rem; }.svadmin-ai-schema-part__parameter strong { border-color: color-mix(in srgb, var(--destructive, currentColor) 35%, transparent); color: var(--destructive, currentColor); }.svadmin-ai-schema-part__parameter p { margin: 0; color: var(--muted-foreground, currentColor); font-size: .72rem; }</style>
