<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import { useSchemaDisplayContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLSpanElement>, 'children' | 'class'> { class?: string; children?: Snippet; } let { class: className = '', children, ...rest }: Props = $props(); const schema = useSchemaDisplayContext('SchemaDisplayMethod');</script>
<span {...rest} class={cn('svadmin-ai-schema-part__method', `svadmin-ai-schema-part__method--${schema.method.toLowerCase()}`, className)} data-slot="schema-display-method">{#if children}{@render children()}{:else}{schema.method}{/if}</span>
<style>.svadmin-ai-schema-part__method { display: inline-flex; min-height: 1.4rem; align-items: center; padding: .1rem .4rem; border-radius: min(var(--radius, .5rem), .3rem); background: var(--muted, transparent); color: var(--muted-foreground, currentColor); font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .68rem; font-weight: 650; }.svadmin-ai-schema-part__method--get { background: color-mix(in srgb, var(--success, currentColor) 12%, transparent); color: var(--success, currentColor); }.svadmin-ai-schema-part__method--post { background: color-mix(in srgb, var(--info, var(--primary, currentColor)) 12%, transparent); color: var(--info, var(--primary, currentColor)); }.svadmin-ai-schema-part__method--put, .svadmin-ai-schema-part__method--patch { background: color-mix(in srgb, var(--warning, currentColor) 12%, transparent); color: var(--warning, currentColor); }.svadmin-ai-schema-part__method--delete { background: color-mix(in srgb, var(--destructive, currentColor) 12%, transparent); color: var(--destructive, currentColor); }</style>
