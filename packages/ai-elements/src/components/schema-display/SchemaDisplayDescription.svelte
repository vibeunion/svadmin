<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import { useSchemaDisplayContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLParagraphElement>, 'children' | 'class'> { class?: string; children?: Snippet; } let { class: className = '', children, ...rest }: Props = $props(); const schema = useSchemaDisplayContext('SchemaDisplayDescription');</script>
{#if children || schema.description}<p {...rest} class={cn('svadmin-ai-schema-part__description', className)} data-slot="schema-display-description">{#if children}{@render children()}{:else}{schema.description}{/if}</p>{/if}
<style>.svadmin-ai-schema-part__description { margin: 0; border-bottom: 1px solid var(--border, currentColor); padding: .75rem; color: var(--muted-foreground, currentColor); font-size: .78rem; line-height: 1.5; }</style>
