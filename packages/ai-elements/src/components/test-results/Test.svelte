<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import TestDuration from './TestDuration.svelte'; import TestName from './TestName.svelte'; import type { TestResultStatus } from './TestResults.svelte'; import TestStatus from './TestStatus.svelte'; import { provideTestContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLDivElement>, 'children' | 'class'> { name: string; status: TestResultStatus; duration?: number; class?: string; children?: Snippet; } let { name, status, duration, class: className = '', children, ...rest }: Props = $props(); provideTestContext({ get name() { return name; }, get status() { return status; }, get duration() { return duration; } });</script>
<div {...rest} class={cn('svadmin-ai-tests-part__test', className)} data-slot="test">{#if children}{@render children()}{:else}<TestStatus /><TestName />{#if duration !== undefined}<TestDuration />{/if}{/if}</div>
<style>.svadmin-ai-tests-part__test { display: flex; flex-wrap: wrap; align-items: center; gap: .45rem; padding: .55rem .75rem; font-size: .75rem; }</style>
