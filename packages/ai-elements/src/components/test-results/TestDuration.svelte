<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import { formatTestDuration, useTestContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLSpanElement>, 'children' | 'class'> { class?: string; children?: Snippet<[number]>; } let { class: className = '', children, ...rest }: Props = $props(); const test = useTestContext('TestDuration');</script>
{#if test.duration !== undefined}<span {...rest} class={cn('svadmin-ai-tests-part__test-duration', className)} data-slot="test-duration">{#if children}{@render children(test.duration)}{:else}{formatTestDuration(test.duration)}{/if}</span>{/if}
<style>.svadmin-ai-tests-part__test-duration { margin-left: auto; color: var(--muted-foreground, currentColor); font-size: .68rem; font-variant-numeric: tabular-nums; }</style>
