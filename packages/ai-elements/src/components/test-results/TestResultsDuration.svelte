<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import { formatTestDuration, useTestResultsContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLSpanElement>, 'children' | 'class'> { class?: string; children?: Snippet<[number]>; } let { class: className = '', children, ...rest }: Props = $props(); const results = useTestResultsContext('TestResultsDuration'); const duration = $derived(results.summary?.duration);</script>
{#if duration !== undefined}<span {...rest} class={cn('svadmin-ai-tests-part__duration', className)} data-slot="test-results-duration">{#if children}{@render children(duration)}{:else}{formatTestDuration(duration)}{/if}</span>{/if}
<style>.svadmin-ai-tests-part__duration { color: var(--muted-foreground, currentColor); font-size: .72rem; font-variant-numeric: tabular-nums; }</style>
