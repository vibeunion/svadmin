<script lang="ts">import type { HTMLAttributes } from 'svelte/elements'; import { ChevronDown } from '@lucide/svelte'; import { cn } from '../../utils.js'; import { useStackTraceContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLDivElement>, 'class'> { class?: string; } let { class: className = '', ...rest }: Props = $props(); const stack = useStackTraceContext('StackTraceExpandButton');</script>
<div {...rest} class={cn('svadmin-ai-stack-part__expand', stack.open && 'svadmin-ai-stack-part__expand--open', className)} data-slot="stack-trace-expand-button" aria-hidden="true"><ChevronDown size={15} /></div>
<style>.svadmin-ai-stack-part__expand { display: inline-flex; width: 1.9rem; height: 1.9rem; flex: none; align-items: center; justify-content: center; color: var(--muted-foreground, currentColor); transition: transform 160ms ease; }.svadmin-ai-stack-part__expand--open { transform: rotate(180deg); }</style>
