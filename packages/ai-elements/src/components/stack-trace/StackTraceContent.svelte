<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { cn } from '../../utils.js'; import { useStackTraceContext } from './context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLDivElement>, 'children' | 'class'> { class?: string; children?: Snippet; maxHeight?: number; } let { class: className = '', children, maxHeight = 400, ...rest }: Props = $props(); const stack = useStackTraceContext('StackTraceContent');</script>
{#if stack.open}<div {...rest} class={cn('svadmin-ai-stack-part__content', className)} style={`max-height: ${maxHeight}px`} data-slot="stack-trace-content">{@render children?.()}</div>{/if}
<style>.svadmin-ai-stack-part__content { overflow: auto; border-top: 1px solid var(--border, currentColor); background: color-mix(in srgb, var(--muted, transparent) 30%, transparent); }</style>
