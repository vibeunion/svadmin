<script lang="ts">import type { Snippet } from 'svelte'; import { cn } from '../../utils.js'; let { name, version, class: className = '', children, ...rest }: { name: string; version?: string; class?: string; children?: Snippet<[string, string | undefined]>; [key: string]: unknown } = $props();</script>
<div class={cn('flex items-center justify-between gap-3 text-sm', className)} {...rest}>{#if children}{@render children(name, version)}{:else}<span class="truncate font-mono text-muted-foreground">{name}</span>{#if version}<span class="shrink-0 font-mono text-xs">{version}</span>{/if}{/if}</div>
