<script lang="ts">import type { Snippet } from 'svelte'; import { Package } from '@lucide/svelte'; import { cn } from '../../utils.js'; import { usePackageInfoContext } from './context.svelte.js'; let { class: className = '', children, ...rest }: { class?: string; children?: Snippet<[string]>; [key: string]: unknown } = $props(); const context = usePackageInfoContext();</script>
<div class={cn('flex min-w-0 items-center gap-2', className)} {...rest}><Package class="size-4 shrink-0 text-muted-foreground" aria-hidden="true" /><span class="truncate font-mono text-sm font-medium">{#if children}{@render children(context.name)}{:else}{context.name}{/if}</span></div>
