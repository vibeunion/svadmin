# @svadmin/devtools-contract

Shared JSON-safe contracts for svadmin and SupaCloud DevTools adapters.

The package has no transport, database, logging, or UI dependency. It defines
trace/correlation metadata, diagnostics, events, snapshots, and recursive
redaction for credentials and signed URLs.
