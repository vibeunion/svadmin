name: Admin UI design assets
on:
  push:
    branches: [main, feat/admin-ui-design-kit-20260919]
    paths:
      - 'design/admin-ui/**'
      - 'packages/ui/src/components.css'
      - 'packages/ui/src/recipes.ts'
      - '.github/workflows/admin-ui-design-assets.yml'
  pull_request:
    branches: [main]
    paths:
      - 'design/admin-ui/**'
      - 'packages/ui/src/components.css'
      - 'packages/ui/src/recipes.ts'
      - '.github/workflows/admin-ui-design-assets.yml'
permissions:
  contents: read
concurrency:
  group: admin-ui-assets-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  design-assets:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 2
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '22'
          package-manager-cache: false
      - name: Test actual source and design metadata
        run: |
          set -euo pipefail
          mkdir -p test-results/admin-ui-design-kit/logs
          node --test design/admin-ui/build.test.mjs design/admin-ui/preview/model.test.mjs 2>&1 | tee test-results/admin-ui-design-kit/logs/tests.log
      - name: Generate source-bound snapshots and check reproducibility
        run: |
          set -euo pipefail
          node design/admin-ui/build.mjs 2>&1 | tee test-results/admin-ui-design-kit/logs/generate.log
          node design/admin-ui/build.mjs --check 2>&1 | tee test-results/admin-ui-design-kit/logs/check.log
          git diff --exit-code -- packages bun.lock
          git diff --check HEAD^ HEAD
      - name: Record source and handoff provenance
        if: always()
        run: |
          set -euo pipefail
          mkdir -p test-results/admin-ui-design-kit/handoff
          cp design/admin-ui/*.json design/admin-ui/README.md test-results/admin-ui-design-kit/handoff/
          git rev-parse HEAD HEAD^{tree} > test-results/admin-ui-design-kit/revision.txt
          node --version > test-results/admin-ui-design-kit/node-version.txt
          find design/admin-ui -type f -print0 | sort -z | xargs -0 sha256sum > test-results/admin-ui-design-kit/source-hashes.txt
          # 只归档该修订的受版本控制源码；不含凭证、环境变量或依赖目录。
          git archive --format=tar.gz HEAD > test-results/admin-ui-design-kit/source.tar.gz
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: admin-ui-design-assets-${{ github.sha }}
          path: test-results/admin-ui-design-kit/
          retention-days: 14
          if-no-files-found: error
