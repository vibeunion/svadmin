diff --git a/dist/main/lib/webauthn.d.ts b/dist/main/lib/webauthn.d.ts
index 52871231b278088e0cac078acdfc0cf7dc9744da..d6b6e7552abd28b564fff75aab121b3d8f9660d9 100644
--- a/dist/main/lib/webauthn.d.ts
+++ b/dist/main/lib/webauthn.d.ts
@@ -72,6 +72,8 @@ export declare function deserializeCredentialRequestOptions(options: ServerCrede
  * Can be either a registration or authentication response
  */
 export type ServerCredentialResponse = RegistrationResponseJSON | AuthenticationResponseJSON;
+export type SerializableRegistrationCredential = Pick<RegistrationCredential, 'id' | 'response' | 'getClientExtensionResults' | 'authenticatorAttachment'> & { toJSON?: () => RegistrationResponseJSON };
+export type SerializableAuthenticationCredential = Pick<AuthenticationCredential, 'id' | 'response' | 'getClientExtensionResults' | 'authenticatorAttachment'> & { toJSON?: () => AuthenticationResponseJSON };
 /**
  * Convert a registration/enrollment credential response to server format.
  * Serializes binary fields to base64url for JSON transmission.
@@ -81,7 +83,7 @@ export type ServerCredentialResponse = RegistrationResponseJSON | Authentication
  * @returns {RegistrationResponseJSON} JSON-serializable credential for server
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
-export declare function serializeCredentialCreationResponse(credential: RegistrationCredential): RegistrationResponseJSON;
+export declare function serializeCredentialCreationResponse(credential: SerializableRegistrationCredential): RegistrationResponseJSON;
 /**
  * Convert an authentication/verification credential response to server format.
  * Serializes binary fields to base64url for JSON transmission.
@@ -91,7 +93,7 @@ export declare function serializeCredentialCreationResponse(credential: Registra
  * @returns {AuthenticationResponseJSON} JSON-serializable credential for server
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
-export declare function serializeCredentialRequestResponse(credential: AuthenticationCredential): AuthenticationResponseJSON;
+export declare function serializeCredentialRequestResponse(credential: SerializableAuthenticationCredential): AuthenticationResponseJSON;
 /**
  * A simple test to determine if a hostname is a properly-formatted domain name.
  * Considers localhost valid for development environments.
diff --git a/dist/main/lib/webauthn.dom.d.ts b/dist/main/lib/webauthn.dom.d.ts
index 2b004e46b6a469572989fed4e10c644fff42fa3d..f5440abc54a297c5bfa4cd5d5c6c4ccf3c4faddf 100644
--- a/dist/main/lib/webauthn.dom.d.ts
+++ b/dist/main/lib/webauthn.dom.d.ts
@@ -223,7 +223,7 @@ export interface RegistrationResponseJSON {
      * The results of processing client extensions.
      * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-getclientextensionresults W3C - getClientExtensionResults}
      */
-    clientExtensionResults: AuthenticationExtensionsClientOutputs;
+    clientExtensionResults: AuthenticationExtensionsClientOutputsJSON;
     /**
      * The type of the credential (always "public-key").
      * @see {@link https://w3c.github.io/webauthn/#dom-credential-type W3C - type}
@@ -271,7 +271,7 @@ export interface AuthenticationResponseJSON {
      * The results of processing client extensions.
      * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-getclientextensionresults W3C - getClientExtensionResults}
      */
-    clientExtensionResults: AuthenticationExtensionsClientOutputs;
+    clientExtensionResults: AuthenticationExtensionsClientOutputsJSON;
     /**
      * The type of the credential (always "public-key").
      * @see {@link https://w3c.github.io/webauthn/#dom-credential-type W3C - type}
@@ -297,22 +297,19 @@ export interface AuthenticatorAttestationResponseJSON {
     attestationObject: Base64URLString;
     /**
      * The authenticator data contained within the attestation object.
-     * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getauthenticatordata W3C - getAuthenticatorData}
      */
-    authenticatorData?: Base64URLString;
+    authenticatorData: Base64URLString;
     /**
      * The transports that the authenticator supports.
-     * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-gettransports W3C - getTransports}
      */
-    transports?: AuthenticatorTransportFuture[];
+    transports: AuthenticatorTransportFuture[];
     /**
      * The COSEAlgorithmIdentifier for the public key.
-     * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getpublickeyalgorithm W3C - getPublicKeyAlgorithm}
      */
-    publicKeyAlgorithm?: COSEAlgorithmIdentifier;
+    publicKeyAlgorithm: COSEAlgorithmIdentifier;
     /**
      * The public key in base64url format.
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getpublickey W3C - getPublicKey}
diff --git a/dist/main/lib/webauthn.js b/dist/main/lib/webauthn.js
index 35e1c8972bda4bbeeaebff7c5dcae60862d0e59a..49ff3c8a0d05c3c796b195f9909f7c65eded87f7 100644
--- a/dist/main/lib/webauthn.js
+++ b/dist/main/lib/webauthn.js
@@ -13,6 +13,7 @@ exports.mergeCredentialCreationOptions = mergeCredentialCreationOptions;
 exports.mergeCredentialRequestOptions = mergeCredentialRequestOptions;
 const tslib_1 = require("tslib");
 const base64url_1 = require("./base64url");
+const webauthn_serialization_1 = require("./webauthn.serialization");
 const errors_1 = require("./errors");
 const helpers_1 = require("./helpers");
 const webauthn_errors_1 = require("./webauthn.errors");
@@ -167,25 +168,33 @@ function deserializeCredentialRequestOptions(options) {
  * @returns {RegistrationResponseJSON} JSON-serializable credential for server
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
+function credentialAttachment(value) {
+    if (value === null) return undefined;
+    if (value === 'platform' || value === 'cross-platform') return value;
+    throw new TypeError('Invalid WebAuthn authenticator attachment');
+}
 function serializeCredentialCreationResponse(credential) {
-    var _a;
     // Check if the credential instance has the toJSON method
     if ('toJSON' in credential && typeof credential.toJSON === 'function') {
         // Use the native WebAuthn Level 3 method
         return credential.toJSON();
     }
-    const credentialWithAttachment = credential;
+    const authenticatorAttachment = credentialAttachment(credential.authenticatorAttachment);
+    const publicKey = credential.response.getPublicKey();
     return {
         id: credential.id,
         rawId: credential.id,
         response: {
             attestationObject: (0, base64url_1.bytesToBase64URL)(new Uint8Array(credential.response.attestationObject)),
             clientDataJSON: (0, base64url_1.bytesToBase64URL)(new Uint8Array(credential.response.clientDataJSON)),
+            authenticatorData: (0, base64url_1.bytesToBase64URL)(new Uint8Array(credential.response.getAuthenticatorData())),
+            publicKeyAlgorithm: credential.response.getPublicKeyAlgorithm(),
+            transports: credential.response.getTransports(),
+            ...(publicKey === null ? {} : { publicKey: (0, base64url_1.bytesToBase64URL)(new Uint8Array(publicKey)) }),
         },
         type: 'public-key',
-        clientExtensionResults: credential.getClientExtensionResults(),
-        // Convert null to undefined and cast to AuthenticatorAttachment type
-        authenticatorAttachment: ((_a = credentialWithAttachment.authenticatorAttachment) !== null && _a !== void 0 ? _a : undefined),
+        clientExtensionResults: (0, webauthn_serialization_1.serializeExtensionResults)(credential.getClientExtensionResults(), base64url_1.bytesToBase64URL),
+        ...(authenticatorAttachment === undefined ? {} : { authenticatorAttachment }),
     };
 }
 /**
@@ -198,17 +207,13 @@ function serializeCredentialCreationResponse(credential) {
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
 function serializeCredentialRequestResponse(credential) {
-    var _a;
     // Check if the credential instance has the toJSON method
     if ('toJSON' in credential && typeof credential.toJSON === 'function') {
         // Use the native WebAuthn Level 3 method
         return credential.toJSON();
     }
-    // Fallback to manual conversion for browsers that don't support toJSON
-    // Access authenticatorAttachment via type assertion to handle TypeScript version differences
-    // @simplewebauthn/types includes this property but base TypeScript 4.7.4 doesn't
-    const credentialWithAttachment = credential;
-    const clientExtensionResults = credential.getClientExtensionResults();
+    const authenticatorAttachment = credentialAttachment(credential.authenticatorAttachment);
+    const clientExtensionResults = (0, webauthn_serialization_1.serializeExtensionResults)(credential.getClientExtensionResults(), base64url_1.bytesToBase64URL);
     const assertionResponse = credential.response;
     return {
         id: credential.id,
@@ -217,14 +222,13 @@ function serializeCredentialRequestResponse(credential) {
             authenticatorData: (0, base64url_1.bytesToBase64URL)(new Uint8Array(assertionResponse.authenticatorData)),
             clientDataJSON: (0, base64url_1.bytesToBase64URL)(new Uint8Array(assertionResponse.clientDataJSON)),
             signature: (0, base64url_1.bytesToBase64URL)(new Uint8Array(assertionResponse.signature)),
-            userHandle: assertionResponse.userHandle
-                ? (0, base64url_1.bytesToBase64URL)(new Uint8Array(assertionResponse.userHandle))
-                : undefined,
+            ...(assertionResponse.userHandle === null ? {} : {
+                userHandle: (0, base64url_1.bytesToBase64URL)(new Uint8Array(assertionResponse.userHandle)),
+            }),
         },
         type: 'public-key',
         clientExtensionResults,
-        // Convert null to undefined and cast to AuthenticatorAttachment type
-        authenticatorAttachment: ((_a = credentialWithAttachment.authenticatorAttachment) !== null && _a !== void 0 ? _a : undefined),
+        ...(authenticatorAttachment === undefined ? {} : { authenticatorAttachment }),
     };
 }
 /**
diff --git a/dist/main/lib/webauthn.serialization.d.ts b/dist/main/lib/webauthn.serialization.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..88c62588e70e3810289c796b2ceef9cca6fb694b
--- /dev/null
+++ b/dist/main/lib/webauthn.serialization.d.ts
@@ -0,0 +1,7 @@
+type Encode = (bytes: Uint8Array<ArrayBuffer>) => string;
+type ExtensionJSON = AuthenticationExtensionsClientOutputsJSON & {
+    hmacCreateSecret?: boolean;
+};
+/** Serialize only supported native WebAuthn extensions, without retaining raw buffers. */
+export declare function serializeExtensionResults(value: unknown, encode: Encode): ExtensionJSON;
+export {};
diff --git a/dist/main/lib/webauthn.serialization.js b/dist/main/lib/webauthn.serialization.js
new file mode 100644
index 0000000000000000000000000000000000000000..bbd2f7032e36ad14131f7bbbc120825f037e69d4
--- /dev/null
+++ b/dist/main/lib/webauthn.serialization.js
@@ -0,0 +1,82 @@
+"use strict";
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.serializeExtensionResults = serializeExtensionResults;
+function invalid() {
+    throw new TypeError('Invalid WebAuthn extension results');
+}
+function object(value, keys) {
+    if (typeof value !== 'object' || value === null || Array.isArray(value))
+        return invalid();
+    const prototype = Object.getPrototypeOf(value);
+    if (prototype !== null && prototype !== Object.prototype)
+        return invalid();
+    const entries = [];
+    for (const key of Reflect.ownKeys(value)) {
+        if (typeof key !== 'string' || !keys.includes(key))
+            return invalid();
+        const descriptor = Object.getOwnPropertyDescriptor(value, key);
+        if (!descriptor || !('value' in descriptor) || !descriptor.enumerable)
+            return invalid();
+        const field = descriptor.value;
+        entries.push([key, field]);
+    }
+    return Object.fromEntries(entries);
+}
+function boolean(value) {
+    if (value === undefined || typeof value === 'boolean')
+        return value;
+    return invalid();
+}
+function binary(value, encode) {
+    if (value instanceof ArrayBuffer)
+        return encode(new Uint8Array(value));
+    if (ArrayBuffer.isView(value) && value.buffer instanceof ArrayBuffer) {
+        return encode(new Uint8Array(value.buffer, value.byteOffset, value.byteLength));
+    }
+    return invalid();
+}
+/** Serialize only supported native WebAuthn extensions, without retaining raw buffers. */
+function serializeExtensionResults(value, encode) {
+    const input = object(value, ['appid', 'credProps', 'hmacCreateSecret', 'largeBlob', 'prf']);
+    const output = {};
+    const appid = boolean(input['appid']);
+    const hmacCreateSecret = boolean(input['hmacCreateSecret']);
+    if (appid !== undefined)
+        output.appid = appid;
+    if (hmacCreateSecret !== undefined)
+        output.hmacCreateSecret = hmacCreateSecret;
+    if (input['credProps'] !== undefined) {
+        const props = object(input['credProps'], ['rk']);
+        const rk = boolean(props['rk']);
+        output.credProps = rk === undefined ? {} : { rk };
+    }
+    if (input['largeBlob'] !== undefined) {
+        const blob = object(input['largeBlob'], ['supported', 'written', 'blob']);
+        const serialized = {};
+        const supported = boolean(blob['supported']);
+        const written = boolean(blob['written']);
+        if (supported !== undefined)
+            serialized.supported = supported;
+        if (written !== undefined)
+            serialized.written = written;
+        if (blob['blob'] !== undefined)
+            serialized.blob = binary(blob['blob'], encode);
+        output.largeBlob = serialized;
+    }
+    if (input['prf'] !== undefined) {
+        const prf = object(input['prf'], ['enabled', 'results']);
+        const serialized = {};
+        const enabled = boolean(prf['enabled']);
+        if (enabled !== undefined)
+            serialized.enabled = enabled;
+        if (prf['results'] !== undefined) {
+            const results = object(prf['results'], ['first', 'second']);
+            const first = binary(results['first'], encode);
+            serialized.results = results['second'] === undefined
+                ? { first }
+                : { first, second: binary(results['second'], encode) };
+        }
+        output.prf = serialized;
+    }
+    return output;
+}
diff --git a/dist/module/lib/webauthn.d.ts b/dist/module/lib/webauthn.d.ts
index 52871231b278088e0cac078acdfc0cf7dc9744da..d6b6e7552abd28b564fff75aab121b3d8f9660d9 100644
--- a/dist/module/lib/webauthn.d.ts
+++ b/dist/module/lib/webauthn.d.ts
@@ -72,6 +72,8 @@ export declare function deserializeCredentialRequestOptions(options: ServerCrede
  * Can be either a registration or authentication response
  */
 export type ServerCredentialResponse = RegistrationResponseJSON | AuthenticationResponseJSON;
+export type SerializableRegistrationCredential = Pick<RegistrationCredential, 'id' | 'response' | 'getClientExtensionResults' | 'authenticatorAttachment'> & { toJSON?: () => RegistrationResponseJSON };
+export type SerializableAuthenticationCredential = Pick<AuthenticationCredential, 'id' | 'response' | 'getClientExtensionResults' | 'authenticatorAttachment'> & { toJSON?: () => AuthenticationResponseJSON };
 /**
  * Convert a registration/enrollment credential response to server format.
  * Serializes binary fields to base64url for JSON transmission.
@@ -81,7 +83,7 @@ export type ServerCredentialResponse = RegistrationResponseJSON | Authentication
  * @returns {RegistrationResponseJSON} JSON-serializable credential for server
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
-export declare function serializeCredentialCreationResponse(credential: RegistrationCredential): RegistrationResponseJSON;
+export declare function serializeCredentialCreationResponse(credential: SerializableRegistrationCredential): RegistrationResponseJSON;
 /**
  * Convert an authentication/verification credential response to server format.
  * Serializes binary fields to base64url for JSON transmission.
@@ -91,7 +93,7 @@ export declare function serializeCredentialCreationResponse(credential: Registra
  * @returns {AuthenticationResponseJSON} JSON-serializable credential for server
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
-export declare function serializeCredentialRequestResponse(credential: AuthenticationCredential): AuthenticationResponseJSON;
+export declare function serializeCredentialRequestResponse(credential: SerializableAuthenticationCredential): AuthenticationResponseJSON;
 /**
  * A simple test to determine if a hostname is a properly-formatted domain name.
  * Considers localhost valid for development environments.
diff --git a/dist/module/lib/webauthn.dom.d.ts b/dist/module/lib/webauthn.dom.d.ts
index 2b004e46b6a469572989fed4e10c644fff42fa3d..f5440abc54a297c5bfa4cd5d5c6c4ccf3c4faddf 100644
--- a/dist/module/lib/webauthn.dom.d.ts
+++ b/dist/module/lib/webauthn.dom.d.ts
@@ -223,7 +223,7 @@ export interface RegistrationResponseJSON {
      * The results of processing client extensions.
      * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-getclientextensionresults W3C - getClientExtensionResults}
      */
-    clientExtensionResults: AuthenticationExtensionsClientOutputs;
+    clientExtensionResults: AuthenticationExtensionsClientOutputsJSON;
     /**
      * The type of the credential (always "public-key").
      * @see {@link https://w3c.github.io/webauthn/#dom-credential-type W3C - type}
@@ -271,7 +271,7 @@ export interface AuthenticationResponseJSON {
      * The results of processing client extensions.
      * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-getclientextensionresults W3C - getClientExtensionResults}
      */
-    clientExtensionResults: AuthenticationExtensionsClientOutputs;
+    clientExtensionResults: AuthenticationExtensionsClientOutputsJSON;
     /**
      * The type of the credential (always "public-key").
      * @see {@link https://w3c.github.io/webauthn/#dom-credential-type W3C - type}
@@ -297,22 +297,19 @@ export interface AuthenticatorAttestationResponseJSON {
     attestationObject: Base64URLString;
     /**
      * The authenticator data contained within the attestation object.
-     * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getauthenticatordata W3C - getAuthenticatorData}
      */
-    authenticatorData?: Base64URLString;
+    authenticatorData: Base64URLString;
     /**
      * The transports that the authenticator supports.
-     * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-gettransports W3C - getTransports}
      */
-    transports?: AuthenticatorTransportFuture[];
+    transports: AuthenticatorTransportFuture[];
     /**
      * The COSEAlgorithmIdentifier for the public key.
-     * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getpublickeyalgorithm W3C - getPublicKeyAlgorithm}
      */
-    publicKeyAlgorithm?: COSEAlgorithmIdentifier;
+    publicKeyAlgorithm: COSEAlgorithmIdentifier;
     /**
      * The public key in base64url format.
      * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getpublickey W3C - getPublicKey}
diff --git a/dist/module/lib/webauthn.js b/dist/module/lib/webauthn.js
index 574fb876f9a7adc661804b3f460f413c2a9471ab..af0b21479fc82117d77557988f26e8658381b006 100644
--- a/dist/module/lib/webauthn.js
+++ b/dist/module/lib/webauthn.js
@@ -1,5 +1,6 @@
 import { __rest } from "tslib";
 import { base64UrlToUint8Array, bytesToBase64URL } from './base64url';
+import { serializeExtensionResults } from './webauthn.serialization';
 import { AuthError, AuthUnknownError, isAuthError } from './errors';
 import { isBrowser } from './helpers';
 import { identifyAuthenticationError, identifyRegistrationError, isWebAuthnError, WebAuthnError, WebAuthnUnknownError, } from './webauthn.errors';
@@ -150,25 +151,33 @@ export function deserializeCredentialRequestOptions(options) {
  * @returns {RegistrationResponseJSON} JSON-serializable credential for server
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
+function credentialAttachment(value) {
+    if (value === null) return undefined;
+    if (value === 'platform' || value === 'cross-platform') return value;
+    throw new TypeError('Invalid WebAuthn authenticator attachment');
+}
 export function serializeCredentialCreationResponse(credential) {
-    var _a;
     // Check if the credential instance has the toJSON method
     if ('toJSON' in credential && typeof credential.toJSON === 'function') {
         // Use the native WebAuthn Level 3 method
         return credential.toJSON();
     }
-    const credentialWithAttachment = credential;
+    const authenticatorAttachment = credentialAttachment(credential.authenticatorAttachment);
+    const publicKey = credential.response.getPublicKey();
     return {
         id: credential.id,
         rawId: credential.id,
         response: {
             attestationObject: bytesToBase64URL(new Uint8Array(credential.response.attestationObject)),
             clientDataJSON: bytesToBase64URL(new Uint8Array(credential.response.clientDataJSON)),
+            authenticatorData: bytesToBase64URL(new Uint8Array(credential.response.getAuthenticatorData())),
+            publicKeyAlgorithm: credential.response.getPublicKeyAlgorithm(),
+            transports: credential.response.getTransports(),
+            ...(publicKey === null ? {} : { publicKey: bytesToBase64URL(new Uint8Array(publicKey)) }),
         },
         type: 'public-key',
-        clientExtensionResults: credential.getClientExtensionResults(),
-        // Convert null to undefined and cast to AuthenticatorAttachment type
-        authenticatorAttachment: ((_a = credentialWithAttachment.authenticatorAttachment) !== null && _a !== void 0 ? _a : undefined),
+        clientExtensionResults: serializeExtensionResults(credential.getClientExtensionResults(), bytesToBase64URL),
+        ...(authenticatorAttachment === undefined ? {} : { authenticatorAttachment }),
     };
 }
 /**
@@ -181,17 +190,13 @@ export function serializeCredentialCreationResponse(credential) {
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
 export function serializeCredentialRequestResponse(credential) {
-    var _a;
     // Check if the credential instance has the toJSON method
     if ('toJSON' in credential && typeof credential.toJSON === 'function') {
         // Use the native WebAuthn Level 3 method
         return credential.toJSON();
     }
-    // Fallback to manual conversion for browsers that don't support toJSON
-    // Access authenticatorAttachment via type assertion to handle TypeScript version differences
-    // @simplewebauthn/types includes this property but base TypeScript 4.7.4 doesn't
-    const credentialWithAttachment = credential;
-    const clientExtensionResults = credential.getClientExtensionResults();
+    const authenticatorAttachment = credentialAttachment(credential.authenticatorAttachment);
+    const clientExtensionResults = serializeExtensionResults(credential.getClientExtensionResults(), bytesToBase64URL);
     const assertionResponse = credential.response;
     return {
         id: credential.id,
@@ -200,14 +205,13 @@ export function serializeCredentialRequestResponse(credential) {
             authenticatorData: bytesToBase64URL(new Uint8Array(assertionResponse.authenticatorData)),
             clientDataJSON: bytesToBase64URL(new Uint8Array(assertionResponse.clientDataJSON)),
             signature: bytesToBase64URL(new Uint8Array(assertionResponse.signature)),
-            userHandle: assertionResponse.userHandle
-                ? bytesToBase64URL(new Uint8Array(assertionResponse.userHandle))
-                : undefined,
+            ...(assertionResponse.userHandle === null ? {} : {
+                userHandle: bytesToBase64URL(new Uint8Array(assertionResponse.userHandle)),
+            }),
         },
         type: 'public-key',
         clientExtensionResults,
-        // Convert null to undefined and cast to AuthenticatorAttachment type
-        authenticatorAttachment: ((_a = credentialWithAttachment.authenticatorAttachment) !== null && _a !== void 0 ? _a : undefined),
+        ...(authenticatorAttachment === undefined ? {} : { authenticatorAttachment }),
     };
 }
 /**
diff --git a/dist/module/lib/webauthn.serialization.d.ts b/dist/module/lib/webauthn.serialization.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..88c62588e70e3810289c796b2ceef9cca6fb694b
--- /dev/null
+++ b/dist/module/lib/webauthn.serialization.d.ts
@@ -0,0 +1,7 @@
+type Encode = (bytes: Uint8Array<ArrayBuffer>) => string;
+type ExtensionJSON = AuthenticationExtensionsClientOutputsJSON & {
+    hmacCreateSecret?: boolean;
+};
+/** Serialize only supported native WebAuthn extensions, without retaining raw buffers. */
+export declare function serializeExtensionResults(value: unknown, encode: Encode): ExtensionJSON;
+export {};
diff --git a/dist/module/lib/webauthn.serialization.js b/dist/module/lib/webauthn.serialization.js
new file mode 100644
index 0000000000000000000000000000000000000000..ca2cb907da82efe82d44a456fa86f572b10e08bd
--- /dev/null
+++ b/dist/module/lib/webauthn.serialization.js
@@ -0,0 +1,79 @@
+function invalid() {
+    throw new TypeError('Invalid WebAuthn extension results');
+}
+function object(value, keys) {
+    if (typeof value !== 'object' || value === null || Array.isArray(value))
+        return invalid();
+    const prototype = Object.getPrototypeOf(value);
+    if (prototype !== null && prototype !== Object.prototype)
+        return invalid();
+    const entries = [];
+    for (const key of Reflect.ownKeys(value)) {
+        if (typeof key !== 'string' || !keys.includes(key))
+            return invalid();
+        const descriptor = Object.getOwnPropertyDescriptor(value, key);
+        if (!descriptor || !('value' in descriptor) || !descriptor.enumerable)
+            return invalid();
+        const field = descriptor.value;
+        entries.push([key, field]);
+    }
+    return Object.fromEntries(entries);
+}
+function boolean(value) {
+    if (value === undefined || typeof value === 'boolean')
+        return value;
+    return invalid();
+}
+function binary(value, encode) {
+    if (value instanceof ArrayBuffer)
+        return encode(new Uint8Array(value));
+    if (ArrayBuffer.isView(value) && value.buffer instanceof ArrayBuffer) {
+        return encode(new Uint8Array(value.buffer, value.byteOffset, value.byteLength));
+    }
+    return invalid();
+}
+/** Serialize only supported native WebAuthn extensions, without retaining raw buffers. */
+export function serializeExtensionResults(value, encode) {
+    const input = object(value, ['appid', 'credProps', 'hmacCreateSecret', 'largeBlob', 'prf']);
+    const output = {};
+    const appid = boolean(input['appid']);
+    const hmacCreateSecret = boolean(input['hmacCreateSecret']);
+    if (appid !== undefined)
+        output.appid = appid;
+    if (hmacCreateSecret !== undefined)
+        output.hmacCreateSecret = hmacCreateSecret;
+    if (input['credProps'] !== undefined) {
+        const props = object(input['credProps'], ['rk']);
+        const rk = boolean(props['rk']);
+        output.credProps = rk === undefined ? {} : { rk };
+    }
+    if (input['largeBlob'] !== undefined) {
+        const blob = object(input['largeBlob'], ['supported', 'written', 'blob']);
+        const serialized = {};
+        const supported = boolean(blob['supported']);
+        const written = boolean(blob['written']);
+        if (supported !== undefined)
+            serialized.supported = supported;
+        if (written !== undefined)
+            serialized.written = written;
+        if (blob['blob'] !== undefined)
+            serialized.blob = binary(blob['blob'], encode);
+        output.largeBlob = serialized;
+    }
+    if (input['prf'] !== undefined) {
+        const prf = object(input['prf'], ['enabled', 'results']);
+        const serialized = {};
+        const enabled = boolean(prf['enabled']);
+        if (enabled !== undefined)
+            serialized.enabled = enabled;
+        if (prf['results'] !== undefined) {
+            const results = object(prf['results'], ['first', 'second']);
+            const first = binary(results['first'], encode);
+            serialized.results = results['second'] === undefined
+                ? { first }
+                : { first, second: binary(results['second'], encode) };
+        }
+        output.prf = serialized;
+    }
+    return output;
+}
diff --git a/src/lib/webauthn.dom.ts b/src/lib/webauthn.dom.ts
index 01f37fffe1784b4332a39b11e17cf04dfb007704..feab4035690107f9a70832a86c7b0da94d6df650 100644
--- a/src/lib/webauthn.dom.ts
+++ b/src/lib/webauthn.dom.ts
@@ -235,7 +235,7 @@ export interface RegistrationResponseJSON {
    * The results of processing client extensions.
    * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-getclientextensionresults W3C - getClientExtensionResults}
    */
-  clientExtensionResults: AuthenticationExtensionsClientOutputs
+  clientExtensionResults: AuthenticationExtensionsClientOutputsJSON
   /**
    * The type of the credential (always "public-key").
    * @see {@link https://w3c.github.io/webauthn/#dom-credential-type W3C - type}
@@ -286,7 +286,7 @@ export interface AuthenticationResponseJSON {
    * The results of processing client extensions.
    * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-getclientextensionresults W3C - getClientExtensionResults}
    */
-  clientExtensionResults: AuthenticationExtensionsClientOutputs
+  clientExtensionResults: AuthenticationExtensionsClientOutputsJSON
   /**
    * The type of the credential (always "public-key").
    * @see {@link https://w3c.github.io/webauthn/#dom-credential-type W3C - type}
@@ -313,22 +313,19 @@ export interface AuthenticatorAttestationResponseJSON {
   attestationObject: Base64URLString
   /**
    * The authenticator data contained within the attestation object.
-   * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
    * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getauthenticatordata W3C - getAuthenticatorData}
    */
-  authenticatorData?: Base64URLString
+  authenticatorData: Base64URLString
   /**
    * The transports that the authenticator supports.
-   * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
    * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-gettransports W3C - getTransports}
    */
-  transports?: AuthenticatorTransportFuture[]
+  transports: AuthenticatorTransportFuture[]
   /**
    * The COSEAlgorithmIdentifier for the public key.
-   * Optional in L2, but becomes required in L3. Play it safe until L3 becomes Recommendation
    * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getpublickeyalgorithm W3C - getPublicKeyAlgorithm}
    */
-  publicKeyAlgorithm?: COSEAlgorithmIdentifier
+  publicKeyAlgorithm: COSEAlgorithmIdentifier
   /**
    * The public key in base64url format.
    * @see {@link https://w3c.github.io/webauthn/#dom-authenticatorattestationresponse-getpublickey W3C - getPublicKey}
diff --git a/src/lib/webauthn.serialization.ts b/src/lib/webauthn.serialization.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9c4f610dcb5c1aa6816a690291aa17d0ede90af0
--- /dev/null
+++ b/src/lib/webauthn.serialization.ts
@@ -0,0 +1,74 @@
+type Encode = (bytes: Uint8Array<ArrayBuffer>) => string
+type ExtensionJSON = AuthenticationExtensionsClientOutputsJSON & { hmacCreateSecret?: boolean }
+
+function invalid(): never {
+  throw new TypeError('Invalid WebAuthn extension results')
+}
+
+function object(value: unknown, keys: readonly string[]): Record<string, unknown> {
+  if (typeof value !== 'object' || value === null || Array.isArray(value)) return invalid()
+  const prototype: unknown = Object.getPrototypeOf(value)
+  if (prototype !== null && prototype !== Object.prototype) return invalid()
+  const entries: [string, unknown][] = []
+  for (const key of Reflect.ownKeys(value)) {
+    if (typeof key !== 'string' || !keys.includes(key)) return invalid()
+    const descriptor = Object.getOwnPropertyDescriptor(value, key)
+    if (!descriptor || !('value' in descriptor) || !descriptor.enumerable) return invalid()
+    const field: unknown = descriptor.value
+    entries.push([key, field])
+  }
+  return Object.fromEntries(entries)
+}
+
+function boolean(value: unknown): boolean | undefined {
+  if (value === undefined || typeof value === 'boolean') return value
+  return invalid()
+}
+
+function binary(value: unknown, encode: Encode): string {
+  if (value instanceof ArrayBuffer) return encode(new Uint8Array(value))
+  if (ArrayBuffer.isView(value) && value.buffer instanceof ArrayBuffer) {
+    return encode(new Uint8Array(value.buffer, value.byteOffset, value.byteLength))
+  }
+  return invalid()
+}
+
+/** Serialize only supported native WebAuthn extensions, without retaining raw buffers. */
+export function serializeExtensionResults(value: unknown, encode: Encode): ExtensionJSON {
+  const input = object(value, ['appid', 'credProps', 'hmacCreateSecret', 'largeBlob', 'prf'])
+  const output: ExtensionJSON = {}
+  const appid = boolean(input['appid'])
+  const hmacCreateSecret = boolean(input['hmacCreateSecret'])
+  if (appid !== undefined) output.appid = appid
+  if (hmacCreateSecret !== undefined) output.hmacCreateSecret = hmacCreateSecret
+  if (input['credProps'] !== undefined) {
+    const props = object(input['credProps'], ['rk'])
+    const rk = boolean(props['rk'])
+    output.credProps = rk === undefined ? {} : { rk }
+  }
+  if (input['largeBlob'] !== undefined) {
+    const blob = object(input['largeBlob'], ['supported', 'written', 'blob'])
+    const serialized: AuthenticationExtensionsLargeBlobOutputsJSON = {}
+    const supported = boolean(blob['supported'])
+    const written = boolean(blob['written'])
+    if (supported !== undefined) serialized.supported = supported
+    if (written !== undefined) serialized.written = written
+    if (blob['blob'] !== undefined) serialized.blob = binary(blob['blob'], encode)
+    output.largeBlob = serialized
+  }
+  if (input['prf'] !== undefined) {
+    const prf = object(input['prf'], ['enabled', 'results'])
+    const serialized: AuthenticationExtensionsPRFOutputsJSON = {}
+    const enabled = boolean(prf['enabled'])
+    if (enabled !== undefined) serialized.enabled = enabled
+    if (prf['results'] !== undefined) {
+      const results = object(prf['results'], ['first', 'second'])
+      const first = binary(results['first'], encode)
+      serialized.results = results['second'] === undefined
+        ? { first }
+        : { first, second: binary(results['second'], encode) }
+    }
+    output.prf = serialized
+  }
+  return output
+}
diff --git a/src/lib/webauthn.ts b/src/lib/webauthn.ts
index e568f4565565afae9185d594756e114cd7bc208d..6a1e6396c386ad228122b8b3891eaff58c0306cc 100644
--- a/src/lib/webauthn.ts
+++ b/src/lib/webauthn.ts
@@ -1,5 +1,6 @@
 import GoTrueClient from '../GoTrueClient'
 import { base64UrlToUint8Array, bytesToBase64URL } from './base64url'
+import { serializeExtensionResults } from './webauthn.serialization'
 import { AuthError, AuthUnknownError, isAuthError } from './errors'
 import {
   AuthMFAEnrollWebauthnResponse,
@@ -242,6 +243,20 @@ export function deserializeCredentialRequestOptions(
  */
 export type ServerCredentialResponse = RegistrationResponseJSON | AuthenticationResponseJSON
 
+export type SerializableRegistrationCredential = Pick<
+  RegistrationCredential, 'id' | 'response' | 'getClientExtensionResults' | 'authenticatorAttachment'
+> & { toJSON?: () => RegistrationResponseJSON }
+
+export type SerializableAuthenticationCredential = Pick<
+  AuthenticationCredential, 'id' | 'response' | 'getClientExtensionResults' | 'authenticatorAttachment'
+> & { toJSON?: () => AuthenticationResponseJSON }
+
+function credentialAttachment(value: string | null): AuthenticatorAttachment | undefined {
+  if (value === null) return undefined
+  if (value === 'platform' || value === 'cross-platform') return value
+  throw new TypeError('Invalid WebAuthn authenticator attachment')
+}
+
 /**
  * Convert a registration/enrollment credential response to server format.
  * Serializes binary fields to base64url for JSON transmission.
@@ -252,17 +267,15 @@ export type ServerCredentialResponse = RegistrationResponseJSON | Authentication
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
 export function serializeCredentialCreationResponse(
-  credential: RegistrationCredential
+  credential: SerializableRegistrationCredential
 ): RegistrationResponseJSON {
   // Check if the credential instance has the toJSON method
   if ('toJSON' in credential && typeof credential.toJSON === 'function') {
     // Use the native WebAuthn Level 3 method
-    return (credential as RegistrationCredential).toJSON()
-  }
-  const credentialWithAttachment = credential as PublicKeyCredential & {
-    response: AuthenticatorAttestationResponse
-    authenticatorAttachment?: string | null
+    return credential.toJSON()
   }
+  const authenticatorAttachment = credentialAttachment(credential.authenticatorAttachment)
+  const publicKey = credential.response.getPublicKey()
 
   return {
     id: credential.id,
@@ -270,13 +283,14 @@ export function serializeCredentialCreationResponse(
     response: {
       attestationObject: bytesToBase64URL(new Uint8Array(credential.response.attestationObject)),
       clientDataJSON: bytesToBase64URL(new Uint8Array(credential.response.clientDataJSON)),
+      authenticatorData: bytesToBase64URL(new Uint8Array(credential.response.getAuthenticatorData())),
+      publicKeyAlgorithm: credential.response.getPublicKeyAlgorithm(),
+      transports: credential.response.getTransports(),
+      ...(publicKey === null ? {} : { publicKey: bytesToBase64URL(new Uint8Array(publicKey)) }),
     },
     type: 'public-key',
-    clientExtensionResults: credential.getClientExtensionResults(),
-    // Convert null to undefined and cast to AuthenticatorAttachment type
-    authenticatorAttachment: (credentialWithAttachment.authenticatorAttachment ?? undefined) as
-      | AuthenticatorAttachment
-      | undefined,
+    clientExtensionResults: serializeExtensionResults(credential.getClientExtensionResults(), bytesToBase64URL),
+    ...(authenticatorAttachment === undefined ? {} : { authenticatorAttachment }),
   }
 }
 
@@ -290,23 +304,16 @@ export function serializeCredentialCreationResponse(
  * @see {@link https://w3c.github.io/webauthn/#dom-publickeycredential-tojson W3C WebAuthn Spec - toJSON}
  */
 export function serializeCredentialRequestResponse(
-  credential: AuthenticationCredential
+  credential: SerializableAuthenticationCredential
 ): AuthenticationResponseJSON {
   // Check if the credential instance has the toJSON method
   if ('toJSON' in credential && typeof credential.toJSON === 'function') {
     // Use the native WebAuthn Level 3 method
-    return (credential as AuthenticationCredential).toJSON()
-  }
-
-  // Fallback to manual conversion for browsers that don't support toJSON
-  // Access authenticatorAttachment via type assertion to handle TypeScript version differences
-  // @simplewebauthn/types includes this property but base TypeScript 4.7.4 doesn't
-  const credentialWithAttachment = credential as PublicKeyCredential & {
-    response: AuthenticatorAssertionResponse
-    authenticatorAttachment?: string | null
+    return credential.toJSON()
   }
 
-  const clientExtensionResults = credential.getClientExtensionResults()
+  const authenticatorAttachment = credentialAttachment(credential.authenticatorAttachment)
+  const clientExtensionResults = serializeExtensionResults(credential.getClientExtensionResults(), bytesToBase64URL)
   const assertionResponse = credential.response
 
   return {
@@ -316,16 +323,13 @@ export function serializeCredentialRequestResponse(
       authenticatorData: bytesToBase64URL(new Uint8Array(assertionResponse.authenticatorData)),
       clientDataJSON: bytesToBase64URL(new Uint8Array(assertionResponse.clientDataJSON)),
       signature: bytesToBase64URL(new Uint8Array(assertionResponse.signature)),
-      userHandle: assertionResponse.userHandle
-        ? bytesToBase64URL(new Uint8Array(assertionResponse.userHandle))
-        : undefined,
+      ...(assertionResponse.userHandle === null ? {} : {
+        userHandle: bytesToBase64URL(new Uint8Array(assertionResponse.userHandle)),
+      }),
     },
     type: 'public-key',
     clientExtensionResults,
-    // Convert null to undefined and cast to AuthenticatorAttachment type
-    authenticatorAttachment: (credentialWithAttachment.authenticatorAttachment ?? undefined) as
-      | AuthenticatorAttachment
-      | undefined,
+    ...(authenticatorAttachment === undefined ? {} : { authenticatorAttachment }),
   }
 }
 
