# Example 核查整改执行记录

## 执行约定

- 来源：2026-09-21 PM 核查；用户后续明确指令“修复所有”，将只读审计转为实施。
- 目标：关闭三份分报告中的全部可修复问题；补齐组件、页面、Lite 和脚手架的真实操作及诚实演示边界。
- 非目标：恢复 Panda、接入未授权生产服务、自动提交/发布/部署、重写 vendor。
- 验收：逐项缺陷映射、针对性确定测试、受影响 example 类型/构建检查、桌面/手机及关键失败路径浏览器复核。
- 风险：中等，共享 UI 与 example 跨模块；真实业务授权与服务端安全接入不在本轮。
- 编排：managed，主任务负责 Office、资源装配及最终集成；三条不重叠写入范围分别为 example 页面、UI 组件、Lite example/生成模板。保持既有脏工作区改动。
- 当前状态：审计发现的本地可修复项已实施，完成直接相关测试与关键交互验收。没有把路由加载通过等同全部业务状态通过；真实服务及生产发布不在本轮。

## 验收跟踪

### Office 与资源入口

- 已修复：核对未改值与改值分别提示；关联预警变为待核实，不再声称已核实。
- 已修复：独立复核可有原因地退回原负责人，保留处置证据及版本/范围/禁止自审门禁。
- 已修复：宿主侧栏与历史返回的未保存保护；单独改变处置结论也计入未保存。
- 已修复：工作台排序统一按等级与到期；报告阻塞直达数据核对、校对、关联预警；补充核对列表和审计列表空态。
- 已修复：去掉 Office 大面板套小卡与负字距；移动端按实际收起动画完成后检查。
- 已修复：`case_workspace`、`design_principles`、`health_office` 显式禁用无意义的通用 CRUD。
- 确定验证：`bun test example/src/office/domain.test.ts`，30 通过、0 失败。
- 浏览器：13 项关键场景通过、无 pageerror，包括取消/确认宿主导航、历史返回/前进、复核退回、手机布局、三个禁用创建入口；14 个 Office 子视图另外完成桌面/手机加载检查。
- 证据：`output/playwright/pm-remediation-20260921/office-interactions.json`、`office-results.json` 及同目录截图。
- 独立只读复核发现取消前进后的历史栈污染，已修复并由复核者验证原生 Navigation index 及无该 API 的自有 marker 两条路径，未发现新问题。

### 共享 UI 与示例装配

- 共享组件逐项实现和 134 项相关单文件测试结果见 [builtin-remediation.md](builtin-remediation.md)。
- 主任务已装配 example 实际成员目录和按演示账户隔离的本机通知偏好；不提供假邀请发送。Provider 单文件测试 5 通过，包含移除外部头像依赖后使用姓名回退。
- 主任务修复 example 数据源吞掉持久化失败的问题：存储写入失败必须拒绝且不污染内存快照。数据库边界单文件测试 9 通过。
- 浏览器完成偏好持久化/刷新恢复、保存失败保值、实际成员目录、无 MFA 服务不造恢复码、翻译与版本文本、JSON 成员导入实际写入等 6 组验证，0 pageerror。
- 证据：`output/playwright/pm-remediation-20260921/builtin-results.json` 及截图。

### Lite 与脚手架

详见 [lite-remediation.md](lite-remediation.md)，包括 dev 和 preview 各 211 路由、原生无脚本表单失败回归、模板真实挂载及构建/检查结果。没有接入真实外部 API。

### 业务工作区

- 逐项源码修复及测试结果见 [business-remediation.md](business-remediation.md)。业务页面专项 19/19；案件 4/4、用户管理 4/4、设计原则 1/1。
- 主任务浏览器复验：Todo 完成后刷新持久化、completed 筛选；AI 从新会话调用本地只读 MCP，返回实际两条低库存记录。
- Mail：草稿收起再展开保留主题；校验失败不丢输入；保存后进入草稿目录可见；本地发送后 Sent 可见；取消切换文件夹仍在原页并保留未保存主题。
- Case：未受理不能跳到报告；无试验记录不能提交；无图片和倍率不能确认；补齐本地证据和两项模拟审核后可记录模拟交付。上传使用本次生成的界面截图，不含真实业务信息。
- 手机 Calendar、Todo、CRM 的统计改为紧凑摘要；在最终构建截图中人工复核，日历切月及待办主区域已进入首屏。
- 证据：`output/playwright/pm-remediation-20260921/case-evidence-gate.png`、`case-delivery.png`、`mail-unsaved-protection.png`；最终首屏复拍在 `output/playwright/pm-remediation-final-20260921/spa/`。
- 独立只读复验：房产搜索及收藏/取消收藏实际生效；CRM 完成跟进后待办 2→1，详情与重返列表保持一致；详情返回恢复 `view=saved` 和 `view=notes`。最终冻结构建的新上下文为 0 浏览器错误、0 HTTP >=400；证据 `output/playwright/independent-5191/final-results.json`。此前构建替换期间出现过旧 chunk 404，最终复验未复现；仍有非错误级 Svelte 开发警告。

### 共享组件追加修复

- MemberDirectory 原 `min-w-lg` 被 clean-flat 全局表格规则覆盖，导致手机邮箱过度换行。局部 scoped CSS 修复后实际表格 576px、容器 358px，容器可滚动，document 不溢出。
- 四个成员入口均复验搜索无匹配、清空恢复四人；只读演示不显示无效邀请表单。
- 两个 members 旧别名补齐 canonical 面包屑。精确自定义菜单优先，只在未命中时映射明确别名，不重定向或模糊匹配。直接回归 5/5。
- 最后一次 UI 打包和 example 构建后，5191 对四个成员地址再次完成桌面/手机复拍，旧别名已显示完整面包屑；最终截图在 `output/playwright/pm-members-final-20260921/`。

## 浏览器覆盖与证据解释

- 5190 是本工作树重新启动的开发服务，完成 SPA 337 路由和 Office 14 子视图的桌面/手机扫描，无 pageerror、无 document 横溢出。
- [最终路由证据索引](remediation-routes.csv) 合并原轮、36 项构建复拍及最后 4 项成员入口复拍：351 项均有内容、无 pageerror、无 document 横溢出；每行保留实际来源地址和截图路径，不伪造为同一轮运行。Lite 211 项的证据单独在 Lite 报告，不混入这一索引。
- 扫描期间仍有业务收尾修改，故在代码冻结后对 36 个相关路由使用 5191 构建预览重新采集；这些复拍无空内容、无 pageerror、无 document 横溢出。目录为 `pm-remediation-final-20260921`。
- 首轮 `/design_principles` 冷加载截图为空，不能计为合格截图；独立复扫和构建预览复拍均有完整内容。Office 首次交互检查遇到加载超过 5 秒，重跑 13 项通过，未通过放宽业务断言掩盖失败。
- 5173 存在旧 Vite 缓存，5174 属于其他工作树，两者不作为此次最新业务修复的验收依据。
- 人工查看 13 张联系表所覆盖的 146 个非 CRUD/Office 视图，并对手机日历、任务、CRM、成员表格等重点放大复核；不是逐张人工确认全部 CRUD 图。
- Lite 各 211 路由是独立开发/预览加载检查；原始逐路由明细未落盘，已保存当时命令及原始汇总输出。不能据首页 `scripts:0` 推导所有页面均无脚本。
- 自动截图及浏览器输出位于被忽略的 `output/playwright/`，本机可读但不会自动进入提交；交接时需另行归档。

## 验收边界

后续用户要求继续关闭遗留风险；本节中的旧限制由 [收尾报告](followup.md) 及其最新证据更新。此前截图和路由检查仍保留实际轮次，不冒充本轮重新全扫。

最终质量检查：`bun run --cwd example check` 为 0 errors、0 warnings；`bun run --cwd example build` 成功；`git diff --check` 通过。业务专项在最终集成后重跑 19/19。构建仍有既有大 chunk 和 KaTeX 静态/动态混用提示，未将其描述为无警告构建。

本地体验入口：`http://127.0.0.1:5191/`，对应最终 example 构建预览，不是发布环境。登录页预填演示账号。

- 未恢复 Panda，未提交、推送、发布或部署；未运行全仓测试套件，也没有宣称远端 CI 已通过。
- MFA、组织设置、邀请、真实消息投递等服务未接入时保持明确的能力不可用状态。AI 为本地只读数据助手，邮件为本地示例记录，Case 为页面会话模拟，不能冒充生产能力。
- 邮件移动和本地发送已改为单次本地数据库提交，源版本/指纹拒绝复用 ID 与过期请求；不承诺跨标签并发事务或真实邮件投递。追加回归和浏览器证据见收尾报告。
- 验证代表路由、关键交互及针对性失败分支，不穷尽所有身份、记录、并发与故障组合。Office 跨工作区历史现统一标记，并补验无 Navigation API 时取消前进/返回后的稿件保留。
