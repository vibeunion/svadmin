# 产品页面与嵌套表单整合

## 范围与来源

本次按用户“继续完成”的要求，把已审阅分支合成同一个可验收版本。
主线为 #443 合并提交 `626a85c8a541af295f1db9dee022007a2bf107ca`，
产品来源为 #438 的 `b9f01e1af5eb97744b3808990f7574050e2830a8`，
嵌套表单来源为 #445 的 `03c78b91b28413989aefa11b576d37143c0d0765`。
使用真实父提交保留历史；不纳入 #433 发布、#434 标量替换或 #446 专用产品。

## 冲突取舍

保留产品分支七组命名 recipes、六个公开组件、状态语义颜色、19 状态、
三个视口及中英文/明暗矩阵。运行时 token 来源和历史 Figma 来源继续分离，
代码合并不更新 Figma 已同步状态。

保留主线任务/认证查询、上传取消与重入、异步树/筛选和工作区回归。
Core 的源码接线测试不导入样式编译器；WorkspaceLayout 的 grid、
顶部对齐、默认单栏、桌面双栏与次序断言在真正的 UI recipe/CSS 套件验证。
合入新状态颜色测试而不删掉已有断言，同时保留并行修复新增的完整 columns
对象、默认顶部对齐发布 CSS 与 $derived 接线断言。

嵌套表单九个文件与页面改造不重叠，原样保留：只填缺失默认值、类型化枚举、
独立 JSON 快照、同步提交锁、显式 disabled、失败保留草稿和会话失效保护。
Surface 使用自己的 locale，继续独立执行参数校验、授权、审批与审计。

## 验收要求

之前各分支的通过结果不代表合成版本通过。合成 head 必须重新执行完整 CI/E2E、
严格 Core/UI/Surface 类型、两个 CSS 入口、所有 recipe 断言、真实 OpenUI 解析器、
嵌套 Svelte 表单与打包消费者，以及 228 场景、6 组交互、键盘/状态色浏览器验证。
不放宽任何既有超时、截图相等条件、类型检查、权限或发布门禁。

本地环境只能运行不依赖完整工作区的源码测试；实际结果记录在对应 PR/Actions。
截图需记录来源 checkout，不使用旧图冒充新测试。

## 未声称完成

这不是所有业务路由的视觉重做、完整 JSON Schema 引擎、全量响应式 OpenUI、
真实模型生成准确率测评、生产鉴权或分布式事务，也不是 Figma 同步/无障碍认证。
已有表格 `derived_inert` 警告尚需单独定位；本次不屏蔽警告。
交互入口的协调发布与已发布 peer 下限验证仍独立，不执行发布或部署。
